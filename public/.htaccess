# Redirect www to non-www (works for both HTTP and HTTPS)
# Update the domain name below to match your actual domain
RewriteEngine on
RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
